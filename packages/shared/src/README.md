# Shared Source

Use this folder for small shared TypeScript source files used by the frontend or generated client package.

Keep implementation logic in the owning app or service whenever possible. Backend code belongs in `services/api`.
