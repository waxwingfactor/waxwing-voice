# Repo Scripts

Use this folder for scripts that affect the whole repo.

Service-specific scripts should stay inside:

- `apps/web`
- `services/api`
- `services/voice-agent`

Do not put secrets in scripts.

