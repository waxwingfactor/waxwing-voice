# Environments

Owner: Subbu

Use one folder per environment:

- `local`
- `staging`
- `pilot`

Each environment folder should document:

- Service URLs
- Required secret names
- Provider resources
- Deployment notes
- Monitoring notes

Do not commit secret values.

