# Tests

Use this folder for cross-system tests and shared test fixtures.

Rules:

- Service-specific tests belong inside their service folder.
- End-to-end tests belong in `tests/e2e`.
- Shared sample data belongs in `tests/fixtures`.

