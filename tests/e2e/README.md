# End-to-End Tests

Use this folder for tests that cross service boundaries.

Examples:

- Inbound call creates a call record
- Lead capture appears in dashboard API
- Document upload becomes searchable
- Tour booking creates booking and email records

